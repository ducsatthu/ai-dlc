One-line: a single row in the mixed activity + comms feed; clicking opens the thread or task drawer.

```jsx
<FeedItem time="14:22" id="MSG-0058" from="fe-dev" to="be-dev" type="clarification"
  summary="status enum trả code hay label? i18n phía nào?" />
```

Summaries are one line and truncate — the drawer carries detail. New rows slide in, then go still.
