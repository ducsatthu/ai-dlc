One-line: shows where an intent stands in `1 Request → 8 Release`; the single most repeated element in the tower.

```jsx
<StageStrip current={5} gate="D" />
<StageStrip current={2} labels />
```

Never reorder or rename the stages. The ◇ diamond always means "gate open, waiting on a human".
