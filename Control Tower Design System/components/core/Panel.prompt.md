One-line: the standard bordered surface for every block in the tower and every figure in a document.

```jsx
<Panel title="Gate Queue" meta="2 chờ bạn">…</Panel>
<Panel title="Task board" pad={false}><TaskRow …/></Panel>
```

No shadows anywhere. Panels separate by border + background step (--surface on --bg), not elevation.
