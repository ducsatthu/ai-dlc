One-line: page- or screen-level state pill — one per masthead, never sprinkled in dense rows (use Chip there).

```jsx
<StatusChip tone="gate">BẢN THIẾT KẾ v3 — CHỜ DUYỆT</StatusChip>
```

Tones: gate (amber) · agent (blue) · done (green) · danger · muted.
