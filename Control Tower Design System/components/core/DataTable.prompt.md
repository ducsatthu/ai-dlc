One-line: the roster / decision / gate reference table used throughout the documents.

```jsx
<DataTable columns={['Agent','Stage','Trách nhiệm']} rows={[['be-dev','6 · Construction','API contract, service, migration']]} />
```

Cells are top-aligned; long prose in a cell is normal. Wrap in a scroll container on narrow widths (built in).
