# Overrides đang bật của dự án này
> Mỗi override: file cùng đường dẫn tương đối với bản plugin. Frontmatter bắt buộc:
> `mode: patch|replace` · `reason` · `source: LL-xxx|DEC-xxx` · `created`.
> /dlc-doctor cảnh báo override thiếu nguồn gốc hoặc đã được upstream.

| File | Mode | Nguồn | Lý do |
|---|---|---|---|
| (chưa có) | | | |
